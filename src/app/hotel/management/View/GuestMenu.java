package app.hotel.management.View;

import javax.swing.*;

class GuestMenu extends JFrame {
    public GuestMenu() {
        setTitle("Guest Menu");
        setSize(400, 300);
        setVisible(true);
    }
}